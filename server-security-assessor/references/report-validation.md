# Report-directed validation

Extract each report row with its source page or row, exact asset, port or URL, vulnerability identifier, reported evidence, and proposed verification method. Show ambiguous mappings before execution.

Verify only those rows. Preserve every row in the final report as `confirmed`, `probable`, `not-reproduced`, or `not-assessable`; do not silently drop an unverified finding.

