# 可移植发行包验收

- 发行包：`releases/managebase-linux-x86_64-static.tar.gz`，14,020,405 字节。
- 发行包 SHA256：`fd7bcbe8148408f1a708d438106dc87e2b929a40086709a00de9884e65520212`。
- 应用 `manage.pyz` SHA256：`3f06bf412be1054141995dabc29a0224bfb08ddbccb7def93049c5483b35da9f`。
- 离线运行时 SHA256：`c2c5cb13c56d4a2f75a2f88f3b041a9c1bde7c1107b383c41046a5016d74eea6`。

## 已执行

1. 从 skill 内的管理源码和运行时输入构建发行包，再复制源码到独立 Linux 临时目录重建；两个归档 SHA256 相同。构建使用随包的静态 CPython 3.12.14。
2. 管理回归 **194 项全部通过，0 跳过**，见 `management-regression.json`。包含真实本地 Compose 配置解析；容器、防火墙等变更由 mock 验证，不冒称真实业务变更验收。
3. 空文件系统隔离测试 **22 项通过**，见 `portable-scratch.json`，报告绑定上述发行包 SHA256。没有宿主机 `/usr`、`/lib`、Python、locale/terminfo 或网络，自带静态解释器和终端数据库可用。
4. 验证 ELF 无动态解释器、无共享库依赖；校验包内文件；验证中文与空格目录搬运、PATH 启动、CLI、无 TTY/无状态拒绝、固定安装记录与根目录不匹配拒绝、压缩和 YAML 依赖。
5. 真实 PTY 验证中文显示/输入、方向键、Esc、窗口缩放、小终端退出及终端状态恢复。另有管理回归覆盖 SIGTERM 恢复。
6. 为文件系统时间戳碰撞补充了镜像归档内容 SHA256 校验和回归用例；相同大小/时间戳的改写也必须重新确认，不能进入 Docker 导入。
7. skill 结构校验通过；仅收录通用管理源码、依赖及测试，不收录旧安装器、业务镜像/配置/数据、目标机地址或登录凭据。

## 未执行与限制

- 本静态产物未更新到远程测试机，也未在本次验收中实施真实 Docker/防火墙变更。旧版远程部署测试不是本产物的远程验收证据。
- 当前只验证 Linux x86_64；ARM64、最低内核版本、全部发行版组合未测。
- 仍需目标机 POSIX `/bin/sh`、被管理的 Docker/Compose/本机 socket，以及防火墙功能所需的宿主机后端和权限。
- 本发行包是管理工具，不提供安装/卸载/整包升级、K3s、通用 reload、exposure CRUD 或通用自动 restore。
