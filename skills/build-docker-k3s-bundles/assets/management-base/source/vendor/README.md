# Offline YAML Dependency

Pinned dependency: `ruamel.yaml 0.18.6`, pure Python mode.
Upstream wheel: `ruamel.yaml-0.18.6-py3-none-any.whl` from PyPI.
SHA-256: `57b53ba33def16c4f3d807c0ccbc00f8a6081827e81ba2491691b76882d0c636`.

The extracted upstream source and distribution metadata, including its license,
are retained here. The `ruamel/__init__.py` namespace shim is project-owned.
No C extension or runtime pip installation is required. Packaging includes this
source and its metadata in the management zipapp.
