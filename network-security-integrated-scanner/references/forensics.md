# Forensics

Enter this workflow only when the user selects it directly or confirms an upgrade after suspicious evidence is shown. Collect the minimum relevant process, network, login, persistence, file-metadata, hash, and log-timeline evidence.

Treat artifacts as sensitive. Do not acquire full memory or disk images, deploy agents, quarantine files, or remove malware without separate explicit authorization.

